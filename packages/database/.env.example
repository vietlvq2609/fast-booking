# PostgreSQL connection string (copy to .env and fill in your values)
# Matches the docker-compose.yml defaults at the repo root
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fast_booking?schema=public"
